apply plugin: 'maven'

repositories {
    mavenCentral()
}

group = 'com.ccbfm.journal'
version = '1.0.0'

//上传本地库
uploadArchives{
    repositories{
        mavenDeployer{
            repository(url: uri('../repo'))
        }
    }
}